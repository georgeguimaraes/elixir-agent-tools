StatusWorker polls an external operation until the result is ready. Requests have a fifteen-minute lifetime starting at `requested_at` (Unix seconds). Repair the worker so pending requests stop when that lifetime expires, while ready responses still return their result and temporary fetch errors remain retryable. Keep the existing configurable status client and clock. Run the public tests.
