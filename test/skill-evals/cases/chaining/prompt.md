Finishing an export must save its file path and enqueue a notification as one operation. Repair `SkillEval.Exports.finish/3`: callers should receive `{:ok, export}` only after both steps succeed, and `{:error, changeset}` when either step is invalid. Preserve the pending export if notification scheduling fails. Keep the existing `job_opts` argument because callers use it to customize scheduling. Run the public tests.
